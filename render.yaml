services:
  - type: web
    name: language-exchange-server
    env: node
    buildCommand: npm install --production
    startCommand: node server/index.js
    rootDir: .
    envVars:
      - key: NODE_ENV
        value: production